```python
class SimpleTreeNode:
    """Класс описывает узел простого дерева
 
    Constructor
    -----------
    __init__(self, val=None, parent)
        создает объект класса <SimpleTreeNode> c заданными параметрами
 
    Attributes
    ----------
    IsRoot : bool
        маркер корневого узла (default is False)
 
    NodeValue : None, int, optional
        значение, которое хранится в узле (default is None)
 
    Parent : <SimpleTreeNode>
        указатель на родительский узел
        желателен для быстрого выполнения различных операций
 
    Children : []
        список дочерних узлов
 
    NodeLevel : int
        поддержка уровня узлов без анализа всего дерева (extra task)
        это длина кратчайшего пути из корневого узла в данный, выраженная в количестве ветвей

    """

class SimpleTree:
    """Класс описывает структуру данных дерево

    Constructor
    -----------
    __init__(self, root=None)
        создает объект класса <SimpleTree> c заданными параметрами

    Attributes
    ----------
    Root : <SimpleTreeNode>
        хранит ссылку на корневой узел (default is None)
 
    Methods
    -------
    AddChild(self, ParentNode, NewChild)
        добавить текущему узлу новый узел в качестве дочернего
        тест: проверяем наличие добавленного узла

    DeleteNode(self, NodeToDelete)
        Удалить некорневой узел вместе со всем поддеревом
        тест: проверяем отсутствие удалённого узла и его потомков

    GetAllNodes(self)
        последовательно обойти всё дерево и сформировать список всех узлов в произвольном порядке 

    FindNodesByValue(self, val)
        найти список подходящих узлов по заданному значению
        тест: проверяем результат с тестовым списком

    MoveNode(self, OriginalNode, NewParent)
        переместить некорневой узел дочерним узлом в другое место дерева (вместе с его поддеревом)
        тест: проверяем, что узел отсутствует там где был исходно и появился в новом месте

    Count(self)
        подсчитать общее количество узлов в дереве
        тест: проверяем на контрольном дереве количество узлов 

    LeafCount(self)
        подсчитать общее количество листьев в дереве
        тест: проверяем на контрольном дереве количество листьев 

    """
```
